---
const nav = [
  { href: "/", label: "Home" },
  { href: "/#pricing", label: "Pricing" },
  { href: "/#contact", label: "Contact" },
];
---
<header class="site-header">
  <div class="container navbar">
    <nav class="nav-links">
      {nav.map((n) => <a href={n.href}>{n.label}</a>)}
    </nav>
    <a href="/#contact" class="btn btn-outline cta">Get in touch</a>
  </div>
</header>
