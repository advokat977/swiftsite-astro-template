---
---
<section class="hero" aria-labelledby="hero-title">
  <div class="bg">
    <div class="vignette"></div>
    <div class="gradient"></div>
  </div>

  <div class="wrap">
    <h1 id="hero-title" class="title">
      Your Vision, <span>Live in 72 Hours.</span>
    </h1>
    <p class="lede">
      Streamlined web development. Premium craft, zero hassle.
    </p>

    <div class="cta-row">
      <a class="btn primary" href="#contact">Launch Your Project</a>
      <a class="link" href="#pricing" aria-label="See pricing">See pricing →</a>
    </div>

    <ul class="badges" role="list">
      <li>⚡ Lighthouse 100/100</li>
      <li>🛡️ Cloudflare hosting</li>
      <li>🔧 Zero maintenance</li>
    </ul>
  </div>
</section>

<style>
.hero{position:relative;overflow:hidden}
.bg{position:absolute;inset:0}
.vignette{position:absolute;inset:-8%;background:radial-gradient(80% 65% at 65% 40%,rgba(28,45,56,.25),transparent 65%)}
.gradient{position:absolute;inset:0;background:linear-gradient(180deg,#0f1821 0%, #12161b 38%, #14181c 100%);opacity:.04}
.wrap{position:relative;max-width:1120px;margin:0 auto;padding:96px 18px 56px;color:#121317}
.title{font-size:clamp(34px,5.2vw,64px);line-height:1.08;margin:0 0 10px;font-weight:800;letter-spacing:-.015em}
.title span{color:#b07a3a}
.lede{margin:0 0 20px;color:#4a5563;font-size:clamp(18px,2.2vw,20px)}
.cta-row{display:flex;gap:14px;align-items:center;flex-wrap:wrap;margin-bottom:14px}
.btn.primary{
  background:linear-gradient(180deg,#d9b07a 0%,#b3864f 100%);
  color:#fff;text-decoration:none;font-weight:800;border-radius:14px;padding:12px 18px;
  box-shadow:0 10px 26px rgba(176,125,66,.28)
}
.btn.primary:hover{filter:brightness(.96)}
.link{font-weight:700;color:#1d1f24;text-decoration:none}
.link:hover{text-decoration:underline}
.badges{display:flex;gap:10px;flex-wrap:wrap;margin:8px 0 0;padding:0;list-style:none;color:#5a6472;font-size:14px}
</style>
